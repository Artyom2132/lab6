# Лабораторная работа №6: Интерфейсы и шаблоны ООП

### Задание 1: Кот

#### Шаг 1.1: Создание базового класса `Cat`
- Создал класс с полем для имени и кол-во мяуканий 
- Сделал конструктор с проверкой имени  
- Добавил методы:
  - `ToString()` — для строкового представления  
  - `Meow()` — для однократного мяуканья  
  - `Meow(n)` — для `n`-кратного мяуканья
  - <img width="400" height="106" alt="image" src="https://github.com/user-attachments/assets/c72f11a8-24fa-477b-b5fd-690a0381caac" />


#### Шаг 1.2: Интерфейс `IMeowable`
- Создал интерфейс с методом `Meow()`  
- Реализовал интерфейс в классе `Cat`  
- Создал дополнительный класс `RobotCat` с другой реализацией мяуканья  
- Разработал статический метод для вызова мяуканья у коллекции объектов

#### Шаг 1.3: Подсчёт мяуканий
- Модифицировал класс `Cat` для подсчёта вызовов `Meow()`  
- Создал метод, который вызывает мяуканье 5 раз
- <img width="500" height="400" alt="image" src="https://github.com/user-attachments/assets/600e5d0c-a955-440a-acee-190e46a7b232" />

---

### Задание 2: Дроби

#### Шаг 2.1: Создание класса `Fraction`
- Создал поля для числителя и знаменателя  
- Реализовал конструктор с проверкой знаменателя ≠ 0  
- Реализовал сокращение дроби (через НОД)  
- Перегрузил арифметические операторы: `+`, `-`, `*`, `/` , и те же операции с целыми числами

#### Шаг 2.2: Сравнение дробей
- Переопределил метод `Equals()` для сравнения по значениям числителя и знаменателя  
- Переопределил `GetHashCode()` 

#### Шаг 2.3: Клонирование
- В методе `Clone()` создавется новый объект со зачениями числителя и знаменателя

#### Шаг 2.4: Кэширование
- Создал интерфейс `IFractionOperations` с методами:
  - `GetDoubleValue()` — получение вещественного значения  
  - `SetNumerator()` — установка числителя  
  - `SetDenominator()` — установка знаменателя  

- Создал класс `CachedFraction`:
  - Добавил поле для хранения кэшированного значения  
  - В `GetDoubleValue()` вычисляем значение только при первом вызове  
<img width="800" height="400" alt="image" src="https://github.com/user-attachments/assets/5b483613-56a6-412f-93b7-e937ef335317" />
